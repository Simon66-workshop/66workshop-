import SwiftUI

struct CatBellView: View {
    var body: some View {
        ZStack {
            Capsule().fill(LuckyCatTokens.ColorToken.collarRed).frame(width: 96, height: 12)
            Circle().fill(LuckyCatTokens.ColorToken.gold).frame(width: 28, height: 28)
                .overlay(Circle().stroke(Color.white.opacity(0.45), lineWidth: 1))
            Circle().fill(LuckyCatTokens.ColorToken.goldDeep).frame(width: 5, height: 5).offset(y: 4)
        }
    }
}
